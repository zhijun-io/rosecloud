spring:
  config:
    import:
      - optional:classpath:rosecloud-common.yaml
  application:
    name: rosecloud-monolith
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
    default: dev
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false

server:
  port: 8080

rosecloud:
  cache:
    enabled: true
    type: in-memory
